import { PhotographerHome } from '@/components/photographer/photographer-home';

/** Former photographer home — stats, bookings overview, earnings. */
export default function PhotographerDashboardPage() {
  return <PhotographerHome />;
}
