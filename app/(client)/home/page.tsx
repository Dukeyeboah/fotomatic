import { DashboardHome } from '@/components/dashboard/dashboard-home';

/** Client overview (stats, bookings, updates). Browse lives at /photographers. */
export default function HomeOverviewPage() {
  return <DashboardHome />;
}
